import { createRoot } from 'react-dom/client';
import App from './App';

const container = document.getElementById('root');

createRoot(container).render(<App />); // Al final de App hacer CTRL+SPACE para importarlo (para que se pueda importar tiene que estar abierto como pestaña el archivo App.jsx)
